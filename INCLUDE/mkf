

PRF=$(SPATH)/all/porfuncs.sh
DV=$(SPATH)/all/display_version.sh
HEADER=$(SPATH)/all/header.sh

DVOPTS=fflags "${FFLAGS}" prog $(PROGNAME) fc_full_name "${FULL_COMPILER_NAME}" fc_exec "${FC}" make_opts "${MAKE_OPTS}" 

porfuncs.f90: $(PRF)
	$(PRF) ${SWITCH} > $@

dv.f90: ${DV} 
	${DV} ${DVOPTS} > $@

header.f90: ${HEADER} 
	${HEADER} > $@
